{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "uv sync",
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "command": "uv sync",
            "problemMatcher": []
        },
    ]
}